/**
  * Designates the webhooks in order to receive the incoming emails from the MailGun API! 
  * It's the right way. It does this through polling the mailgun api for more emails 
  * instead of assigning webhooks
  */